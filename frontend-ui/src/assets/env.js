(function (window) {
    window["env"] = window["env"] || {};
    window["env"].BACKEND_CORE_URL = "${BACKEND_CORE_URL}";
    window["env"].BACKEND_ANALYTICS_URL = "${BACKEND_ANALYTICS_URL}";
  })(this);