(function (window) {
    window["env"] = window["env"] || {};
    window["env"].BACKEND_CORE_URL = 'http://localhost:8080'; // not actualized, for local testing
    window["env"].BACKEND_ANALYTICS_URL = 'http://localhost:5000'; // not actualized, for local testing
  })(this);